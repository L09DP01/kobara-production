/* eslint-disable @typescript-eslint/no-explicit-any -- Stored JSON report snapshots are runtime-shaped. */
import { requireAdmin } from '@/lib/auth/require-admin'; import { createAdminClient } from '@/utils/supabase/admin'; import { GenerateReportsButton } from './generate-button';
export default async function ReportsPage(){await requireAdmin(['super_admin','operations']);const s=createAdminClient();const {data}=await s.from('platform_monthly_reports').select('*').order('report_month',{ascending:false});return <div><div className="flex flex-wrap items-end justify-between gap-4"><div><h1 className="text-2xl font-bold">FINANCIAL REPORTS</h1><p className="mt-1 text-sm text-slate-500">Merchant, Developer, Ambassador and platform snapshots.</p></div><GenerateReportsButton/></div><div className="mt-8 overflow-x-auto border border-slate-800"><table className="w-full min-w-[720px] text-left text-xs"><thead className="bg-slate-900 text-slate-500"><tr><th className="p-4">MONTH</th><th>STATUS</th><th>PAYMENTS</th><th>GROSS HTG</th><th>GROSS USD</th><th>FEES HTG</th><th>FEES USD</th><th>EXPORT</th></tr></thead><tbody>{(data||[]).map((r:any)=><tr key={r.id} className="border-t border-slate-800"><td className="p-4">{r.report_month}</td><td>{r.status}</td><td>{r.totals?.payment_count||0}</td><td>{r.totals?.gross_htg||0}</td><td>{r.totals?.gross_usd||0}</td><td>{r.totals?.fees_htg||0}</td><td>{r.totals?.fees_usd||0}</td><td><a className="text-red-400" href={`/api/admin/reports/${r.id}/csv`}>CSV</a></td></tr>)}</tbody></table></div></div>}
