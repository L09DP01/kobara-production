'use client'; import { useState } from 'react'; import { generateReportsNow } from './actions'; export function GenerateReportsButton(){const [message,setMessage]=useState('');return <div><button onClick={async()=>{setMessage('Génération…');const r=await generateReportsNow();setMessage(r.error||'Rapports mis à jour.')}} className="border border-slate-700 px-4 py-2 text-xs font-bold hover:bg-slate-800">GENERATE NOW</button>{message&&<span className="ml-3 text-xs text-slate-400">{message}</span>}</div>}
