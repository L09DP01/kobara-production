import { redirect } from 'next/navigation'; import { getPartnerSession } from '@/lib/server/partners/auth';
export default async function AmbassadorStatusPage(){const p=await getPartnerSession('ambassador',{activeOnly:false});if(!p)redirect('/ambassador/login');if(p.account.status==='active')redirect('/ambassador/portal');return <main className="grid min-h-[100dvh] place-items-center bg-[#07101d] p-5 text-white"><section className="max-w-lg rounded-md border border-slate-800 bg-slate-900 p-8"><p className="text-sm font-bold uppercase text-orange-400">Statut du compte</p><h1 className="mt-3 text-3xl font-black">{p.account.status}</h1><p className="mt-4 text-slate-400">Votre accès Ambassadeur doit être activé par l’administration Kobara.</p><a href="/logout" className="mt-7 inline-block font-bold text-orange-400">Déconnexion</a></section></main>}
