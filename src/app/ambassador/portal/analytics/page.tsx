/* eslint-disable @typescript-eslint/no-explicit-any -- Supabase relationship rows are runtime-shaped. */
import { requirePartner } from '@/lib/server/partners/auth'; import { MetricCard } from '@/components/partners/metric-card';
export default async function AmbassadorAnalytics(){const {account,supabase}=await requirePartner('ambassador');const {data}=await supabase.from('partner_commission_ledger').select('amount,currency,status,source_merchant_id,created_at').eq('ambassador_id',account.id).order('created_at',{ascending:false});const total=(c:string)=>(data||[]).filter((x:any)=>x.currency===c&&x.status!=='reversed').reduce((a:number,x:any)=>a+Number(x.amount),0);return <div><h1 className="text-3xl font-black">Analyses</h1><section className="mt-7 grid gap-4 sm:grid-cols-2"><MetricCard label="Récompenses USD" value={`${total('USD').toFixed(2)} $`}/><MetricCard label="Récompenses HTG" value={`${total('HTG').toFixed(2)} HTG`}/></section><div className="mt-7 flex flex-wrap gap-3"><a href="/api/partners/statements.csv" className="rounded-md border border-slate-700 px-4 py-2 text-sm font-bold">Exporter CSV</a><a href="/api/partners/statements.pdf" className="rounded-md border border-slate-700 px-4 py-2 text-sm font-bold">Exporter PDF</a></div></div>}
