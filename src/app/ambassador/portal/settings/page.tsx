import { requirePartner } from '@/lib/server/partners/auth'; export default async function AmbassadorSettings(){const {account,user}=await requirePartner('ambassador');return <div><h1 className="text-3xl font-black">Paramètres</h1><section className="mt-7 max-w-2xl rounded-md border border-slate-800 bg-slate-900 p-5"><dl className="grid gap-4 sm:grid-cols-2"><div><dt className="text-xs uppercase text-slate-500">Nom</dt><dd className="mt-1">{account.display_name}</dd></div><div><dt className="text-xs uppercase text-slate-500">E-mail</dt><dd className="mt-1">{user.email}</dd></div><div><dt className="text-xs uppercase text-slate-500">Devise</dt><dd className="mt-1">{account.reward_currency}</dd></div><div><dt className="text-xs uppercase text-slate-500">Statut</dt><dd className="mt-1">{account.status}</dd></div></dl></section></div>}
