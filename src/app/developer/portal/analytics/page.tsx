/* eslint-disable @typescript-eslint/no-explicit-any -- Supabase ledger rows are runtime-shaped. */
import { requirePartner } from '@/lib/server/partners/auth'; import { MetricCard } from '@/components/partners/metric-card';
export default async function DeveloperAnalytics(){const {account,supabase}=await requirePartner('developer');const {data}=await supabase.from('partner_commission_ledger').select('amount,currency,status,entry_type,created_at').eq('developer_id',account.id).order('created_at',{ascending:false});const sum=(currency:string,status?:string)=>(data||[]).filter((x:any)=>x.currency===currency&&(!status||x.status===status)).reduce((a:number,x:any)=>a+Number(x.amount),0);return <div><h1 className="text-3xl font-black">Analyses</h1><section className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><MetricCard label="Disponible USD" value={`${sum('USD','available').toFixed(2)} $`}/><MetricCard label="Disponible HTG" value={`${sum('HTG','available').toFixed(2)} HTG`}/><MetricCard label="Total USD" value={`${sum('USD').toFixed(2)} $`}/><MetricCard label="Total HTG" value={`${sum('HTG').toFixed(2)} HTG`}/></section><div className="mt-7 flex flex-wrap gap-3"><a href="/api/partners/statements.csv" className="rounded-md border border-slate-700 px-4 py-2 text-sm font-bold">Exporter CSV</a><a href="/api/partners/statements.pdf" className="rounded-md border border-slate-700 px-4 py-2 text-sm font-bold">Exporter PDF</a></div></div>}
